import { Component } from '@angular/core';

/** The application shell renders the Cognito login form for this public route. */
@Component({
  standalone: true,
  template: '',
})
export class LoginPlaceholderComponent {}
